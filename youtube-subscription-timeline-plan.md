# YouTube チャンネル登録日一覧サイト 実装計画書

## 1. プロジェクト概要

### コンセプト

自分の YouTube 登録チャンネルを「登録した日時」順に一覧化し、「このチャンネル、こんな昔から登録してたぜ」という古参自慢をシェアできるサイト。

- Google ログイン一発で即結果が出る(Takeout 等のアップロード不要)
- **完全クライアント完結**: ユーザーデータはサーバーに一切送信・保存しない
- 結果をカード画像化して SNS(X 等)にシェアできる導線がバズの本体

### バズ仮説

「人の承認欲求に急にリーチするデータ可視化」路線。Google Map タイムライン可視化サイトのバズと同系統。OAuth ワンクリックで摩擦が極小なのが競合(Takeout 系ツール)に対する優位性。

## 2. アーキテクチャ

```
ユーザーのブラウザ ⇄ Google (OAuth 2.0 / YouTube Data API v3)
        │
        └─ Cloudflare Pages(静的ファイル配信のみ)
```

- サーバー・DB・Workers は一切使わない
- データの流れは「Google → ユーザーのブラウザ」のみ。運営インフラをユーザーデータが通らない
- アクセストークンはメモリ上(JS 変数)のみで保持。localStorage / sessionStorage / Cookie に保存しない

## 3. 技術スタック

| レイヤー       | 選定                                       | 備考                                                         |
| -------------- | ------------------------------------------ | ------------------------------------------------------------ |
| フレームワーク | Vue 3 + Vite + TypeScript                  | SPA。SSR 不要                                                |
| スタイリング   | Tailwind CSS                               |                                                              |
| 状態管理       | 素の ref / composable                      | Pinia 不要の規模                                             |
| OAuth          | Google Identity Services (GIS) token model | `google.accounts.oauth2.initTokenClient`。client secret 不要 |
| API            | YouTube Data API v3 `subscriptions.list`   | ブラウザから直接 fetch(CORS 対応済み)                        |
| 画像生成       | html-to-image(または snapdom)              | DOM → PNG をクライアントで生成しダウンロード                 |
| ホスティング   | Cloudflare Pages                           | 無料枠                                                       |
| OGP            | 静的画像 1 枚                              | 動的 OGP はサーバーが必要になるため不採用                    |

## 4. OAuth / API 仕様

### OAuth

- ライブラリ: GIS(`https://accounts.google.com/gsi/client` を script タグで読み込み)
- フロー: token model(implicit 系)。アクセストークンのみ取得、リフレッシュトークンなし
- スコープ: `https://www.googleapis.com/auth/youtube.readonly`(**sensitive scope**)
- トークン有効期限: 約 1 時間。利用シナリオは数分の一発ものなので再ログインで十分
- client_id は公開前提の値。承認済み JavaScript オリジンを本番ドメイン(+ localhost)に限定する

### API 呼び出し

- エンドポイント: `GET https://www.googleapis.com/youtube/v3/subscriptions`
- パラメータ:
  - `part=snippet`
  - `mine=true`
  - `maxResults=50`
  - `pageToken`(ページネーション。`nextPageToken` がなくなるまでループ)
  - `order=alphabetical` 等はあるが、登録日ソートはクライアント側で `snippet.publishedAt` を使って行う
- 使用フィールド:
  - `snippet.publishedAt` … **登録日時(このサイトの核)**
  - `snippet.title` … チャンネル名
  - `snippet.resourceId.channelId` … チャンネルリンク用
  - `snippet.thumbnails` … サムネイル
- クォータコスト: 1 リクエスト(50 件)= 1 ユニット。登録 500 チャンネルのユーザーでも 10 ユニット程度

### エラーハンドリング

- `401` → トークン失効。再ログインを促す
- `403 quotaExceeded` → 「本日分の解析上限に達しました。明日また来てね」画面に優雅にフォールバック(クォータは太平洋時間 0 時リセット)
- `403 subscriptionForbidden` 等 → 登録チャンネル非公開設定のケースを考慮(mine=true なら基本発生しないが念のため)
- 登録 0 件のケースの空状態 UI

## 5. 画面構成

1. **ランディング**
   - キャッチコピー + 「Google でログイン」ボタン
   - 「データはサーバーに保存されません(ブラウザと Google の間で完結)」を目立つ位置に明記
   - プライバシーポリシー / 利用規約へのリンク(フッター)
2. **結果一覧**
   - 登録日昇順(古い順)でチャンネル一覧表示: サムネ、チャンネル名、登録日、「◯年◯ヶ月前」
   - ハイライト表示: 最古の登録チャンネル、登録から N 年もの、年ごとの登録数など
   - 並び替え(古い順 / 新しい順)、簡易フィルタ
3. **シェアカード**
   - 「最古参 TOP5」等をまとめた縦長カード(スマホスクショ / ストーリーズ映え比率)
   - html-to-image で PNG ダウンロード
   - X シェアボタン(テキスト + ハッシュタグのみ。画像はユーザーが添付)
4. **静的ページ**
   - プライバシーポリシー(データ非保存の明記、YouTube API Services 利用の明記、Google プライバシーポリシーへのリンク)
   - 利用規約(YouTube API Services Terms of Service への同意文言を含める)

## 6. デザイン方針

- シェアカードのデザイン品質がバズを左右する最重要要素。実装より先にカードのビジュアル案を複数作って選ぶ
- 「古参度」を強調する演出(例: 登録年ごとのバッジ、タイムライン表示)
- モバイルファースト(流入はほぼスマホの SNS 経由と想定)

## 7. Google Cloud 側の準備タスク

1. GCP プロジェクト作成、YouTube Data API v3 有効化
2. OAuth 同意画面設定(External): アプリ名、ロゴ、サポートメール、ホームページ URL、プライバシーポリシー URL
3. OAuth クライアント ID(Web アプリケーション)作成、承認済み JavaScript オリジン設定
4. **OAuth 検証申請**(sensitive scope の一般公開に必須)
   - 未検証の間はテストユーザー最大 100 人 + 警告画面
   - 審査に数日〜数週間。プライバシーポリシー・スコープ利用目的の説明が必要
5. **クォータ増枠申請**(Audit and Quota Extension Form)
   - デフォルト 10,000 ユニット/日 ≒ 約 1,500 人/日で枯渇する試算
   - 「1 ユーザー平均 ◯ ユニット × 想定 DAU」の計算根拠を添えて申請(例: 100,000 ユニット/日 ≒ 15,000 人/日)
   - 審査は数週間かかる場合があるため OAuth 検証と**並行して**早めに提出
   - 複数プロジェクトでの無料枠の掛け算は規約違反(全プロジェクト停止リスク)なので絶対にやらない

## 8. 開発フェーズ

### Phase 1: 動作検証(テストユーザーモード)

- GCP セットアップ(同意画面はテストモード)
- OAuth ログイン → subscriptions 全件取得 → 素の一覧表示まで
- 自分 + 友人数名で動作確認

### Phase 2: UI / シェアカード

- 一覧 UI・ハイライト演出の作り込み
- シェアカードのデザインと画像生成
- プライバシーポリシー / 利用規約ページ作成
- クォータ枯渇時のフォールバック UI

### Phase 3: 審査

- OAuth 検証申請 + クォータ増枠申請を提出
- 待ち時間で微調整・OGP・アナリティクス(入れる場合は Cloudflare Web Analytics = Cookie レス)

### Phase 4: 公開

- 独自ドメイン設定(任意。pages.dev でも可)
- SNS で初期告知

## 9. リスクと対策

| リスク                               | 対策                                                                                                 |
| ------------------------------------ | ---------------------------------------------------------------------------------------------------- |
| OAuth 検証に落ちる / 長引く          | プライバシーポリシーを丁寧に書く。データ非保存設計は審査でプラス材料。落ちた場合は指摘対応して再申請 |
| バズ時のクォータ枯渇                 | 増枠申請を先行提出。枯渇時フォールバック UI。1 ユーザーあたりのコストが 1〜10 ユニットと軽いのが救い |
| 登録チャンネル数が極端に多いユーザー | ページネーション上限(例: 20 ページ = 1,000 件)を設けて打ち切り + 注記                                |
| snippet.publishedAt の精度           | 古い登録の日時が丸められている等の報告があれば「参考値」注記で対応                                   |
| トークンの扱い                       | メモリ保持のみを徹底。ログアウト時は `google.accounts.oauth2.revoke` を呼ぶ                          |

## 10. コスト

- YouTube Data API: 無料(クォータ制)
- OAuth 検証 / クォータ増枠: 無料
- Cloudflare Pages: 無料
- ドメイン: 年 1,000 円前後(pages.dev なら 0 円)

**金銭コストは実質ゼロ。先払いするのは審査の待ち時間のみ。**

## 11. コーディング規約(実装セッション向け)

- インデントは半角スペース 2 つ
- TypeScript strict モード
- API レスポンスは型定義(`Subscription`, `SubscriptionListResponse` 等)を用意
- トークン・ユーザーデータを永続化するコードを書かないこと(localStorage / Cookie 禁止)
